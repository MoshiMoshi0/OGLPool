/*
 * RandomPolygon.cpp
 *
 *  Created on: 09-04-2013
 *      Author: _CORE7
 */

#include "RandomPolygon.h"

namespace OGLPool {

RandomPolygon::RandomPolygon() {
	/*
	 * vector< vec2 > points;
	 * Delaunay d;
	 * d.traingulate( points );
	 * vector< DEdge > des = d.getDEdges();
	 *
	 * genRandomPoly( des );
	 *
	 * for( int i = 0; i < des.size(); i++ ){
	 * 		vec2 v0 = points[ des[i].s ];
	 * 		vec2 v1 = points[ des[i].t ];
	 * 		this->addEdge( v0, v1 );
	 * }
	 */
}

RandomPolygon::~RandomPolygon() {
}

} /* namespace OGLPool */
